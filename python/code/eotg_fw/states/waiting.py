

def start_waiting(input_variables):
    setup_initialize_neopixels()
    loop_exit = waiting_loop()
    todo = exit_processing(loop_exit)
    if todo = example-a:
        return something
        #this should return back to background if AC is disconnected or if 2x/hold is detected
    elif todo = example-a:
        start_brew(cause)
